apiVersion: v1
kind: Service
metadata:
  name: cloudant-extractor
  labels:
    app: cloudant-extractor
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: cloudant-extractor

# Made with Bob
